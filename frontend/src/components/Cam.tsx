import React, { MutableRefObject, useEffect, useRef } from 'react';
import styled from 'styled-components';

const Container = styled.div`
  width: 100vw;
  height: 100vh;
`;

function Cam(): JSX.Element {
  return <Container>Cam</Container>;
}

export default Cam;
