import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  flex: 1;
  height: 100%;
`;

function ThreadSection(): JSX.Element {
  return <Container>ThreadSection</Container>;
}

export default ThreadSection;
